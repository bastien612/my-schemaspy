Pour Windows

Installer Graphviz.msi en double cliquant dessus.

Rajouter le dossier (Graphviz/bin) au path Windows.

Changer les valeurs d'accés à la DB dans la config.

Lancer la commande :
     java -jar schemaspy-6.0.0.jar -configFile ./schemaspy.properties  
